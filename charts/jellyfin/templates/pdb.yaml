{{- $replicaCount := int .Values.jellyfin.replicaCount }}
{{- if and .Values.jellyfin.pdb.create (or (gt $replicaCount 1) .Values.jellyfin.autoscaling.enabled) }}
apiVersion: {{ include "common.capabilities.policy.apiVersion" . }}
kind: PodDisruptionBudget
metadata:
  name: {{ include "common.names.fullname" . }}
  namespace: {{ include "common.names.namespace" . | quote }}
  labels: {{- include "common.labels.standard" . | nindent 4 }}
    app.kubernetes.io/component: jellyfin
    {{- if .Values.commonLabels }}
    {{- include "common.tplvalues.render" ( dict "value" .Values.commonLabels "context" $ ) | nindent 4 }}
    {{- end }}
  {{- if .Values.commonAnnotations }}
  annotations: {{- include "common.tplvalues.render" ( dict "value" .Values.commonAnnotations "context" $ ) | nindent 4 }}
  {{- end }}
spec:
  {{- if .Values.jellyfin.pdb.minAvailable }}
  minAvailable: {{ .Values.jellyfin.pdb.minAvailable }}
  {{- end  }}
  {{- if .Values.jellyfin.pdb.maxUnavailable }}
  maxUnavailable: {{ .Values.jellyfin.pdb.maxUnavailable }}
  {{- end  }}
  selector:
    matchLabels: {{- include "common.labels.matchLabels" . | nindent 6 }}
      app.kubernetes.io/component: jellyfin
{{- end }}
